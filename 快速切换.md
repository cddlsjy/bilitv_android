## Blbl 项目遥控器和手势快速切换视频修改大纲

### 修改的文件

1. **`app/src/main/java/blbl/cat3399/feature/player/PlayerActivity.kt`** - 主播放器 Activity
2. **`PlayerActivity-sdk21.kt`** - SDK 21 版本播放器 Activity（参考文件）

***

### 一、添加必要的 Import

在文件开头的 import 区域添加以下三个 import（位于 `import kotlin.math.abs` 之后）：

```kotlin
import android.view.GestureDetector
import android.view.MotionEvent
import android.view.ViewConfiguration
```

***

### 二、修改 dispatchKeyEvent 方法中的方向键处理

找到 `when (keyCode)` 语句块中 `KEYCODE_DPAD_UP` 和 `KEYCODE_DPAD_DOWN` 的处理代码。

**修改前的代码（约第 2022-2048 行）：**

```kotlin
KeyEvent.KEYCODE_DPAD_UP -> {
    if (isSidePanelVisible()) return super.dispatchKeyEvent(event)
    // TV-style shortcut: when OSD is hidden, UP directly opens the playlist
    if (osdMode == OsdMode.Hidden) {
        if (showListPanelFromShortcut()) return true
    }
    setControlsVisible(true)
    if (!binding.seekProgress.isFocused) {
        focusSeekBar()
        return true
    }
}

KeyEvent.KEYCODE_DPAD_DOWN -> {
    if (isSidePanelVisible()) return super.dispatchKeyEvent(event)
    if (binding.seekProgress.isFocused) {
        setControlsVisible(true)
        focusFirstControl()
        return true
    }
    if (osdMode == OsdMode.Hidden) {
        setControlsVisible(true)
        focusDownKeyOsdTargetControl()
        return true
    }
}
```

**修改后的代码：**

```kotlin
KeyEvent.KEYCODE_DPAD_UP -> {
    if (isSidePanelVisible()) return super.dispatchKeyEvent(event)
    if (osdMode == OsdMode.Full && binding.seekProgress.isFocused) return super.dispatchKeyEvent(event)
    if (osdMode == OsdMode.Full && (binding.topBar.hasFocus() || binding.bottomBar.hasFocus())) return super.dispatchKeyEvent(event)
    playPrevByPlaybackMode(userInitiated = true)
    return true
}

KeyEvent.KEYCODE_DPAD_DOWN -> {
    if (isSidePanelVisible()) return super.dispatchKeyEvent(event)
    if (osdMode == OsdMode.Full && binding.seekProgress.isFocused) return super.dispatchKeyEvent(event)
    if (osdMode == OsdMode.Full && (binding.topBar.hasFocus() || binding.bottomBar.hasFocus())) return super.dispatchKeyEvent(event)
    playNextByPlaybackMode(userInitiated = true)
    return true
}
```

***

### 三、添加频道键和翻页键支持

在 `KEYCODE_DPAD_DOWN` 处理之后、`KEYCODE_DPAD_CENTER` 之前添加：

```kotlin
KeyEvent.KEYCODE_CHANNEL_UP,
KeyEvent.KEYCODE_PAGE_UP -> {
    if (isSidePanelVisible()) return super.dispatchKeyEvent(event)
    playPrevByPlaybackMode(userInitiated = true)
    return true
}

KeyEvent.KEYCODE_CHANNEL_DOWN,
KeyEvent.KEYCODE_PAGE_DOWN -> {
    if (isSidePanelVisible()) return super.dispatchKeyEvent(event)
    playNextByPlaybackMode(userInitiated = true)
    return true
}
```

***

### 四、在 initControls 方法中添加触摸手势检测

找到 `initControls` 方法，**在方法开头**（第一行）添加手势检测代码，**删除原有的** **`initTouchGestures()`** **调用**（如果存在且方法不存在）。

```kotlin
private fun initControls(engine: BlblPlayerEngine) {
    // === 手势检测代码开始 ===
    val swipeThreshold = ViewConfiguration.get(this).scaledTouchSlop * 4
    val swipeVelocityThreshold = ViewConfiguration.get(this).scaledMinimumFlingVelocity * 2
    val detector = GestureDetector(this, object : GestureDetector.SimpleOnGestureListener() {
        override fun onDown(e: MotionEvent): Boolean = true

        override fun onFling(
            e1: MotionEvent?,
            e2: MotionEvent,
            velocityX: Float,
            velocityY: Float
        ): Boolean {
            if (isSidePanelVisible()) return false
            if (e1 == null) return false
            val dy = e1.y - e2.y
            val dx = e1.x - e2.x
            if (abs(dy) > abs(dx) && abs(dy) > swipeThreshold && abs(velocityY) > swipeVelocityThreshold) {
                val isUp = dy > 0
                if (isUp) {
                    playPrevByPlaybackMode(userInitiated = true)
                } else {
                    playNextByPlaybackMode(userInitiated = true)
                }
                return true
            }
            return false
        }
    })

    val touchListener = View.OnTouchListener { v, event ->
        val handled = detector.onTouchEvent(event)
        if (event.action == MotionEvent.ACTION_UP && handled) v.performClick()
        handled
    }
    binding.playerView.setOnTouchListener(touchListener)
    binding.ijkAspect.setOnTouchListener(touchListener)
    // === 手势检测代码结束 ===

    binding.btnAdvanced.setOnClickListener {
        // ... 原有代码
    }
    // ... 其他原有代码
}
```

***

### 五、注意事项

1. **initTouchGestures() 方法**：如果代码中调用了 `initTouchGestures()` 但该方法不存在，必须删除该调用，并将手势检测代码放在方法开头。
2. **playerGestureDirectionReversed**：参考文件中使用了 `BiliClient.prefs.playerGestureDirectionReversed`，但该字段可能不存在于旧版本中。此字段用于反转手势方向，如果不存在可忽略，使用 `dy > 0` 判断即可。
3. **重复代码**：确保手势检测代码只在 `initControls` 方法开头出现一次，不要重复添加。
4. **播放方法**：`playNextByPlaybackMode()` 和 `playPrevByPlaybackMode()` 方法必须在文件中已实现。

***

### 六、修改清单

| 步骤 | 修改内容                                                      | 位置                  |
| :- | :-------------------------------------------------------- | :------------------ |
| 1  | 添加 GestureDetector, MotionEvent, ViewConfiguration import | 文件开头 import 区域      |
| 2  | 修改 KEYCODE\_DPAD\_UP 处理                                   | dispatchKeyEvent 方法 |
| 3  | 修改 KEYCODE\_DPAD\_DOWN 处理                                 | dispatchKeyEvent 方法 |
| 4  | 添加 KEYCODE\_CHANNEL\_UP/DOWN 和 PAGE\_UP/DOWN              | dispatchKeyEvent 方法 |
| 5  | 添加手势检测代码                                                  | initControls 方法开头   |

***

### 七、验证方法

编译后可通过以下方式验证：

- **遥控器**：按上/下方向键、频道键、翻页键切换视频
- **触摸屏**：在视频画面上快速上下滑动切换视频

<br />

<br />

